# Driver Mobile Legacy

Implementasi React/Expo lama sudah tidak dipakai sebagai jalur resmi.

Gunakan app Flutter di:

- `driver_mobile/`

Catatan:

- folder ini disimpan sementara hanya sebagai referensi migrasi
- jangan pakai folder ini untuk build APK/IPA baru
- build/install resmi sekarang mengikuti dokumentasi di `driver_mobile/README.md`
